{
  "name": "existing-claude-only-fixture",
  "version": "1.0.0",
  "description": "Fixture with pre-existing CLAUDE.md but no AGENTS.md",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "nodemon": "^3.0.0",
    "jest": "^29.0.0"
  }
}
